To test, do:

  twiggy -Ilib bin/webdeck

If you want to test based on the distro without installing, do:

  dzil run 'twiggy bin/webdeck'

But that is a bit annoying if you want to make use of auto-reload.

